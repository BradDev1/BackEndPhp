{% extends 'base.html.twig' %}

{% block title %}Hello ContactController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { display: flex; flex-direction: column; align-items: center; justify-content: center;}
</style>

<div class="example-wrapper">
    {{ dump() }}
    {{form_start(form)}}
    {{form_widget(form)}}
    <input type="submit" placeholder="go">
    {{form_end(form)}}
   
</div>
{% endblock %}
